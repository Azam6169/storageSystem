{% extends "master.html.twig" %}

{% block title %}Files {% if isEditing %}edit{% else %}add{% endif %}{% endblock %}

{% block content %}
    {% if errorList %}
        <ul>
            {% for error in errorList %}
                <li>{{error}}</li>
                {% endfor %}
        </ul>
    {% endif %}
    
    <form method="POST" enctype="multipart/form-data">
            <label>Select your file :</label><br>
            File Name:<input type="text" name="filename" value="{{v.filename}}" ><br>
            <input type="file" name="filename" value="{{f.filename}}"><br>
            <button type="submit">Upload</button>            
           {# <button type="submit" value="{% if isEditing %}Update{% else %}Add{% endif %} file"</button> #}
            <fieldset>
                <legend>Previous uploaded files</legend>
                <ul id="menu">
                    <li><a href="">All files</a></li>
                    <li><a href="">Documents</a></li>
                    <li><a href="">Images</a></li>
                    <li><a href="">Applications</a></li>
                </ul>

                <ul id="files">
                    <?php echo $uploaded_files; ?>
                </ul>
            </fieldset>
        </form>    
{% endblock %}
